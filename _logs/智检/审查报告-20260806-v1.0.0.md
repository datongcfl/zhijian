# 智检 独立代码审查报告

- 日期：2026-08-06
- 版本：v1.0.0
- 审查对象：`f:\_backups\zhijian-20260801`（备份快照，与权威源码 `f:\projects\zhijian\` 逐行一致）
- 方法：静态分析 + 双子代理交叉验证（2/2 确认才纳入）+ 修复后 smoke 验证
- 范围：配置解析、CI 门禁、Git/subprocess、缓存、遥测、ML 模型安全等高危区

## 审查结论

整体工程质量良好，但存在 1 条 Critical 安全漏洞、2 条 Major 一致性问题。修复已应用到 `f:\projects\zhijian\`。

## 发现与修复

| No. | 严重度 | 问题 | 修复 |
|-----|--------|------|------|
| 1 | 🔴 Critical | `ml/classifier.py` 不安全 `pickle.load` 反序列化，加载不可信模型可 RCE | HMAC 签名信封 + 结构校验，旧模型兼容告警 |
| 2 | 🟠 Major | `core.py` avg_inflation/avg_ddc 只遍历 Python `results`，与 avg_ldr/avg_deficit 口径不一致 | 新增 `_result_inflation_score`/`_result_ddc_ratio`，统一遍历 `all_results` |
| 3 | 🟠 Major | `git_hook_runner.py` pre-commit 无一 unbounded 超时，卡死将阻塞 commit | `subprocess.run` 加 `timeout=60` + 捕获输出 |
| 4 | 🟡 Minor | `config.py` `_WeightsSchema` 未校验权重和 | 加 `model_validator` 校验 4 项之和 = 1.0 |
| 5 | 🟡 Minor | `git_hook_runner.py` 丢弃 `argv` 输入参数 | 透传给 CLI |

## 验证证据

- `python -m py_compile`（4 个变更文件）：通过
- 权重和校验：合法＝1.0 放行，≠1.0 拒绝 ✓
- 分类器 save/load 往返一致；篡改 payload 触发 `Model signature mismatch` ✓
- `python -m pytest -q`：**122 passed**
- CLI smoke：`--version` → `zhijian 1.0.0`；HTML 报告生成成功，退出码 0

## 变更文件

- `src/zhijian/ml/classifier.py`
- `src/zhijian/core.py`
- `src/zhijian/git_hook_runner.py`
- `src/zhijian/config.py`

## 遗留说明

- #1 的 HMAC 检测篡改/损坏，需结合"只加载可信模型"；旧模型文件重新保存一次即启用签名。